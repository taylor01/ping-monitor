# =============================================================================
# PING MONITOR CONFIGURATION - Phase 1
# =============================================================================

# -----------------------------------------------------------------------------
# Site Identification (required)
# -----------------------------------------------------------------------------
SITE_NAME=home

# -----------------------------------------------------------------------------
# Rails API Configuration (Phase 1 - optional)
# If not configured, measurements will buffer locally until Rails API is deployed
# -----------------------------------------------------------------------------
# RAILS_API_URL=https://monitoring.yourdomain.com
# SITE_SECRET=your-site-secret-here

# -----------------------------------------------------------------------------
# Datadog Configuration (optional if Rails API configured)
# At least one output (Rails API or Datadog) must be configured
# -----------------------------------------------------------------------------
DD_API_KEY=your_datadog_api_key_here
# DD_SITE=datadoghq.com  # or datadoghq.eu for EU region

# -----------------------------------------------------------------------------
# Ping Settings
# -----------------------------------------------------------------------------
# PING_INTERVAL=60  # Seconds between ping cycles (default: 60)
# PING_COUNT=3      # ICMP packets per host (default: 3)
# PING_TIMEOUT=2    # Timeout per packet in seconds (default: 2)
# MAX_WORKERS=20    # Concurrent Datadog submissions (default: 20)

# -----------------------------------------------------------------------------
# Optional: Central Host Configuration
# -----------------------------------------------------------------------------
# CONFIG_URL=https://config.example.com/hosts.json
