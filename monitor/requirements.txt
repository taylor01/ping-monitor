# ICMP ping library
icmplib>=3.0

# HTTP requests (async)
httpx>=0.27.0

# Legacy sync HTTP (for Datadog until we migrate)
requests>=2.31

# Async SQLite for local buffering
aiosqlite>=0.20.0

# Structured logging with correlation tracking
structlog>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
