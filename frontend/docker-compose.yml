services:
  frontend:
    image: taylor01/ping-monitor-frontend:latest
    # To build locally instead of pulling, comment out 'image' and uncomment 'build':
    # build:
    #   context: .
    #   args:
    #     VITE_API_URL: /api/v1
    #     VITE_SITE_ID: ${SITE_ID:-home}
    ports:
      - "80:80"
    restart: unless-stopped
